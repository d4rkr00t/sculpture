{
  "name": "sculpture",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:cli && npm run build:native",
    "build:cli": "opaline build",
    "build:native": "neon build",
    "dev": "opaline dev",
    "dev:native": "watch 'npm run build:native' native/src",
    "typecheck": "tsc",
    "lint:staged": "lint-staged",
    "prepare": "npm run typecheck"
  },
  "keywords": [],
  "author": "Stanislav Sysoev <@d4rkr00t>",
  "license": "MIT",
  "bin": {
    "sculpture": "dist/cli.js"
  },
  "pre-commit": [
    "lint:staged"
  ],
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "@opaline/core": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^4.1.3",
    "lint-staged": "^10.5.3",
    "pre-commit": "^1.2.2",
    "prettier": "^2.2.1",
    "neon-cli": "^0.7.0",
    "watch": "^1.0.2"
  }
}
